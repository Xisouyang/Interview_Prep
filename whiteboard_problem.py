
def find_mid(ll):
    mid_num = ll.count // 2
    count = 0
    curr_node = ll.head
    while curr_node != None:
        if counter = mid_num:
            break
        curr_node = curr_node.next
        counter += 1

    return (curr_node.data, curr_node.next.data)
